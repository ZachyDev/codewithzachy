/-gets the hire me button-/
const hireMe = document.getElementById('hireme');

/-function inputs-/
const inputSpecs = () => {
    let email = document.getElementById('contactEmail').value;
    alert(`Submission Recorded,Zachy_Dev will respond via ${email}`);
}